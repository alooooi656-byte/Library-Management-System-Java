# Library Management System (OOP - Java)

A simple Java project demonstrating the core concepts of Object-Oriented Programming (OOP), specifically **Inheritance** and **Constructors**.

## 🚀 Features
- **Inheritance**: Uses a base class `LibraryItem` for shared properties.
- **Constructor Chaining**: Demonstrates the use of `super()` to initialize parent data.
- **Static Members**: Tracks the total number of items created using a `static` counter.
- **Method Overriding**: Custom implementation of `displayInfo()` for different item types.

## 🛠️ Technologies Used
- Java JDK 11+
- VS Code (or any Java IDE)

## 📖 How it Works
The system allows you to create `Book` and `Magazine` objects. Each time an object is instantiated, the global counter increases, and you can display detailed info for each item.
